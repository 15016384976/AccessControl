@model AccessControl.Models.Authorize.LogoutModel

@if (!string.IsNullOrEmpty(Model.PostLogoutRedirectUri))
{
    <script>
        window.location.href = '@Model.PostLogoutRedirectUri';
    </script>
}